# Sanity Configuration
# Copy this file to .env.local and fill in your values
# IMPORTANT: Never commit .env.local to git!

# Your Sanity project ID (from https://sanity.io/manage)
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id

# Dataset name (e.g., "prod" or "production")
NEXT_PUBLIC_SANITY_DATASET=prod

# API version (use current date format)
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# Optional: Read token for preview mode (Viewer permission)
# SANITY_API_READ_TOKEN=

# For webhook-based revalidation (generate any random string)
# SANITY_WEBHOOK_SECRET=
